---
# tasks file for roles/Role1
- name: Installing Apache
  apt:
   name: apache2
   state: latest
   update_cache: yes

- name: Configuring firewall to allow apache
  ufw:
   rule: allow
   direction: in
   name: 'Apache'
