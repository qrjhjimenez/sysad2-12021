---
# tasks file for roles/UbuntuInstall

- name: Installing Java for Ubuntu
  apt:
   name: default-jre
   state: latest

- name: Install boto, ansible, and openstack packages
  pip:
   name:
   - boto
   - ansible
   - python-openstackclient
